# react-helmet-async still declares a peer range that stops at React 18, and the app
# runs React 19 — npm install fails on that alone unless peer conflicts are downgraded
# to warnings. This was already how node_modules got built; writing it down means the
# server's install behaves like the one here instead of erroring out mid-deploy.
legacy-peer-deps=true
